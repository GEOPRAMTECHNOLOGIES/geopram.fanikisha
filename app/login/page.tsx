"use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setMsg("");const x=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await x.json();if(!x.ok){setMsg(d.error||"Sign in failed");return}r.push(d.role==="admin"?"/__ADMIN__":"/dashboard")}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="fluent-card w-full max-w-md p-7"><div className="text-xs font-bold uppercase tracking-widest text-blue-700">Secure access</div><h1 className="mt-2 text-2xl font-semibold">Sign in</h1><div className="mt-6"><label className="fluent-label">Email</label><input className="fluent-input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="mt-4"><label className="fluent-label">Password</label><input className="fluent-input" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div>{msg&&<div className="mt-4 rounded-md bg-red-50 p-3 text-sm text-red-700">{msg}</div>}<button className="fluent-primary mt-6 w-full">Continue</button><p className="mt-4 text-xs text-gray-500">Admin access requires the account email to exist in MongoDB with role ADMIN.</p></form></main>}
