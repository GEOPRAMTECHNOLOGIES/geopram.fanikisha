"use client";import {useEffect,useState} from "react";export default function InvoicesPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/client/documents").then(r=>r.json()).then(x=>setRows(x.invoices||[]))},[]);return <main className="content-wrap"><h1 className="text-2xl font-semibold">Invoices</h1>{rows.map(x=><div className="fluent-card section-card mt-3" key={x._id}>{x.number} · {x.status} · {x.currency} {x.amount}</div>)}</main>}
